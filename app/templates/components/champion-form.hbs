<form {{action 'createChampion' on='submit'}}>
  {{input value=newChampion.name placeholder="Champion"}}
  {{input value=newChampion.role placeholder="Role"}}
  {{input value=newChampion.guide placeholder="Guide"}}
  <button type="submit">Save</button>
</form>
